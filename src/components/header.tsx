import React from 'react';
import { CardHeader, CardTitle } from "@/components/ui/card";

export function CardHeaderComponent() {
    return (
        <CardHeader>
            <CardTitle>Resumo de Horas Trabalhadas</CardTitle>
        </CardHeader>
    );
}

